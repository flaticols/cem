name: Publish
on:
  push:
    branches:
      - latest

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v5
      - name: Install dependencies
        run: bun install
      - name: Publish package
        run: npx jsr publish
